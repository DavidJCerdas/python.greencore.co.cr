import logging

# Using Basic Configurations to configure the module:  basicConfig(**kwargs) method
# logging.basicConfig(level=logging.ERROR, filename="app.log", filemode='a')

# Test the basic configuration.
# logging.error('This is the first test logging to a file')

# Let's put some format to the messages
# logging.basicConfig(level=logging.CRITICAL, filename="app.log", filemode='a', format='%(asctime)s: %(name)s - %(levelname)s - %(message)s')

# Test the basic configuration.
# logging.critical('Testing now a critical message with some format')
